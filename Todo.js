//The repeating emails are the sent ones..not the inbox ones so optimise that 
// Read mails data targeting part is kind a hardcoded if in testing it comes out to be faulty then use searchMails func logic
// mails mein parts[0].parts[0] jo hardcode kia hai usko find method se replace kro 
// kch parts parts -> parts 2 level p nested aur kch to teen level p b nested hai parts -> parts -> parts RESOLVED 

// and make the code less repetitive. READ FUNCTIONS SAHI SE OPTIMIZE KRO SARE.
// Refresh token gets expired so resolve that issue..
// handle the refresh token more securely and flow of code optimise kro
// optimise mail viewing draft m b dikkat arhi hai for a draft jisme na koi subject ho na "to". empty values se deal nhi kr parha hai code.
// attachment is of 2 forms url encoding and normal encoding.. url one uses _ - and normal uses + /
// server crashes it needs to restart on its own.
// is authenticateed is not working for the put method routes
// highlightling is working sometimes and sometimes not 

